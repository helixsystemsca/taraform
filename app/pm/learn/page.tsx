/** PM learning flow placeholder. */
export default function PmLearnPage() {
  return (
    <div className="space-y-3">
      <p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-slate-500">Learn</p>
      <h1 className="text-xl font-semibold text-slate-50">Learning system</h1>
      <p className="text-sm text-slate-400">
        Placeholder for the PM learning loop. Hook shared study APIs from here when you implement flows —
        use <code className="rounded bg-slate-800 px-1 text-slate-300">useAppMode()</code> to branch UI only.
      </p>
    </div>
  );
}
